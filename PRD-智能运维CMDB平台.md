# 企业智能运维 CMDB 平台 — 产品需求文档（PRD）

| 文档信息 | 内容 |
|---|---|
| 文档版本 | V0.5（决策完整版，可进入研发排期） |
| 文档状态 | 15 项需求决策已确认（第 11 章）；V0.3 评审补充已 Review 完毕；虚机纳管/事件单/堡垒机三项新决策已固化 |
| 目标读者 | 运维团队、研发团队、部门主管、决策层 |
| 编写说明 | 基于当前网络环境（飞塔防火墙、思科防火墙、H3C 交换机、上网行为管理、Cisco AC 无线）梳理，含补充建议模块 |

---

## 1. 项目背景与目标

### 1.1 公司网络环境现状

当前公司网络由以下设备构成：

| 设备类型 | 品牌/形态 | 在平台中的定位 |
|---|---|---|
| 防火墙 | Fortinet（飞塔 FortiGate） | 网络出口/边界安全，需深度监控（会话、策略、VPN） |
| 防火墙 | Cisco（ASA/FTD） | 边界/区域隔离 |
| 交换机 | H3C（Comware 平台） | 核心/汇聚/接入，数量最多，重点管理对象 |
| 无线 AC 控制器 | Cisco 9800-L（多接口）与 3504（传统）混合部署（已确认） | 无线网络控制，需管理下挂 AP 与终端；两代 WLC 采集方式不同，需双驱动适配 |
| 无线 AP | 各型 AP | 归属 AC 管理，台账需与 AC 自动同步 |
| 上网行为管理 | 深信服 AC（已确认） | 出口链路，关注在线人数与流量排行，SNMP + 深信服开放 API 采集 |
| 光猫、ODF、配线架 | 无源设备 | 仅做台账与物理位置管理，无需监控 |
| 服务器 | 物理机 + 大量虚机/云主机（V0.5 已确认），已部署 Prometheus agent | 物理机/虚机分层建模（宿主机-虚机），监控数据复用不重复建设；虚机清单经 vCenter/云平台 API 同步 |
| （扩展）路由器、PDU、UPS、打印机、安全设备等 | — | 模型需可扩展 |

### 1.2 现状痛点

1. **台账不准**：设备信息分散在多份 Excel，谁改的、什么时候改的无人知晓，设备下架后台账未更新。
2. **信息割裂**：想知道一台设备"配置 + 状态 + 日志 + 所在机柜位置"，需要登录 3~5 个系统或逐台 SSH。
3. **巡检靠人肉**：每天人工登录设备敲命令检查 CPU、接口错包、电源风扇，效率低且容易漏检。
4. **无全局视角**：缺乏拓扑视图，故障定位靠个人记忆和经验，新人上手慢。
5. **变更无留痕**：网络设备配置被改动后无记录、无对比、无法追溯，出了问题查不到原因。
6. **告警不统一**：各设备告警靠各自Web或邮件，缺乏统一告警收敛、通知与闭环处理。
7. **数据孤岛**：服务器有 Prometheus 监控，网络设备靠 SNMP，两套体系割裂，无法统一查看。

### 1.3 建设目标

**一句话定位**：以 CMDB 为底座，打造"看得见、管得住、报得准、自动跑"的 IT 设备管理与 AI 自动化运维平台。

- **一个底座**：统一、准确、自动更新的设备配置数据库（CMDB）；
- **两个视角**：物理视角（机房 → 机柜 → 设备 → 端口）+ 逻辑视角（拓扑、VLAN、路由、无线）；
- **三类数据**：静态配置数据 + 动态运行状态数据 + 日志数据；
- **四个闭环**：监控 → 告警 → 巡检 → 自动化处置的运维闭环；
- **AI 赋能**：大模型驱动的自然语言查询、智能巡检解读、告警根因分析与值班助手。

### 1.4 成功指标（验收参考）

| 指标 | 目标值 |
|---|---|
| 存量设备台账覆盖率 | 100% |
| 网络设备 SNMP 纳管（已确认规模 500+ 台） | 全量开启并接入采集 |
| 台账数据自动更新比例（自动发现回填） | ≥ 80% |
| 日常巡检人工投入 | 减少 90% 以上 |
| 故障平均定位时间（MTTR） | 降低 50% |
| 网络设备配置备份覆盖率 | 100%，配置变更 100% 有记录 |
| 告警通知到达延时 | ≤ 1 分钟 |

---

## 2. 术语定义

| 术语 | 定义 |
|---|---|
| CMDB | 配置管理数据库，本平台的核心底座 |
| CI | 配置项（Configuration Item），一台设备、一条线缆、一个IP均可为CI |
| U | 机柜高度单位，1U ≈ 44.45mm |
| ODF | 光纤配线架 |
| LLDP/CDP | 链路层发现协议（H3C用LLDP，Cisco用CDP），用于自动发现邻居与绘制拓扑 |
| SNMP | 简单网络管理协议，网络设备指标采集的主要手段（v2c/v3） |
| Syslog | 网络设备系统日志外发协议 |
| WLC/AC | 无线控制器 |
| IPAM | IP 地址管理 |
| NCM | 网络配置管理（备份/对比/下发） |
| MTTR | 平均修复时间 |
| RAG | 检索增强生成，大模型结合私有知识库回答的技术 |
| NL2Query | 自然语言转结构化查询 |

---

## 3. 用户与角色

| 角色 | 职责 | 典型使用场景 |
|---|---|---|
| 系统管理员 | 平台全部权限 | 用户/权限/参数管理，模型配置 |
| 网络运维工程师 | 网络设备全生命周期管理 | 台账维护、监控分析、配置备份、批量变更、巡检 |
| 服务器/系统运维 | 服务器与主机管理 | 服务器台账、Prometheus 指标查看 |
| 值班工程师 | 告警处理 | 接收告警、确认、初步处置、转交 |
| 部门主管 | 审批与报表 | 查看资产/容量/告警报表，审批高危操作 |
| 普通员工（只读） | 查询设备信息 | 查设备位置、管理IP、联系人 |
| AI 助手（系统角色） | 通过 API 访问平台数据 | 自然语言查询、报告生成、根因分析 |
| 审计/合规 | 只读审计视角 | 查看变更历史、操作日志 |

> 权限模型必须同时支持 **功能权限**（能访问哪些菜单/按钮）与 **数据权限**（只能看本机房/本站点/本类别设备）。

---

## 4. 总体设计：模块划分

### 4.1 模块全景图

```
┌────────────────────────────────────────────────────────────────┐
│                     统一工作台（Dashboard）                      │
├──────────┬──────────┬──────────┬──────────┬───────────────────┤
│ 系统管理  │  资源管理  │  监控告警  │  运维作业  │    智能与开放     │
│──────────│──────────│──────────│──────────│───────────────────│
│ 用户/角色 │ 机房管理  │ 监控中心  │ 巡检中心  │  AI 智能助手      │
│ 组织架构  │ 机柜可视化 │ 告警中心  │ 拓扑管理  │  (NL查询/根因分析 │
│ 权限RBAC  │ 设备管理  │ 日志中心  │ 自动化运维│   /巡检解读)      │
│ 凭据管理  │ 资产生命周期│          │ 网络配置  │  报表中心        │
│ 审计日志  │ IPAM      │          │ 管理NCM  │  开放API/Webhook │
│ 通知渠道  │ 线缆管理  │          │          │                   │
│          │ 设备使用与 │          │          │                   │
│          │ 共享(5.17)│          │          │                   │
└──────────┴──────────┴──────────┴──────────┴───────────────────┘
        ↕ 采集引擎层：SNMP / SSH(CLI) / 厂商API / Prometheus对接 / Syslog接收
┌────────────────────────────────────────────────────────────────┐
│  存储层：PostgreSQL(主数据) │ 时序库(指标) │ 日志库 │ Redis(队列) │
└────────────────────────────────────────────────────────────────┘
```

### 4.2 模块清单与优先级

> P0 = 一期必须；P1 = 二期；P2 = 三期及以后（增强项）

| 模块 | 说明 | 优先级 | 备注 |
|---|---|---|---|
| 系统管理 | 用户、角色、权限、组织、审计、通知渠道 | P0 | 一切的基础 |
| 凭据管理 | 设备账号密码加密保管 | P0 | **易被遗漏但安全上必须有** |
| 机房管理 | 机房档案 + 机柜可视化（拖拽上架） | P0 | 核心亮点功能 |
| 设备管理（CMDB核心） | 动态模型、设备台账、360°视图 | P0 | 平台底座 |
| 监控中心 | SNMP采集 + Prometheus复用 + 状态总览 | P0 | |
| 告警中心 | 规则、通知、闭环处理 | P0 | **原需求未明确，必须补上** |
| 日志中心 | Syslog接收、设备日志抓取、终端操作日志 | P1 | |
| 巡检中心 | 巡检模板、定时任务、自动报告 | P0 | 核心诉求 |
| 拓扑管理 | 自动发现 + 手工编辑 + 告警联动 | P1 | |
| 网络配置管理（NCM） | 配置备份、版本diff、变更留痕 | P1 | **网络运维刚需，强烈建议** |
| 自动化运维 | 脚本库、批量执行、任务编排 | P2 | 高危能力，需审批流 |
| 资产生命周期 | 采购→上架→维修→报废、保修合同 | P1 | |
| IPAM | 网段与IP管理、ARP/MAC联动 | P1 | |
| 线缆管理 | 接口级物理连线、跳线记录 | P1 | 与机柜可视化配合 |
| AI 智能助手 | 自然语言查询、根因分析、报告解读 | P2（接口一期预留） | 差异化亮点 |
| 报表中心 | 资产/容量/告警报表 | P1 | |
| 开放API | REST API + Webhook | P0（基础CRUD）/P2（全量） | 供AI与其他系统调用 |
| 设备使用与共享（评审补充） | 占用/预约/释放、登录会话监测、远程登录审计、可用资源检索、共享资源池 | P1 | **评审补充，V0.4 Review 已采纳**：支撑测试机/共享设备复用，详见 5.17 |
| 虚拟化管理（V0.5 新增） | 宿主机-虚机建模、vCenter/云平台 API 同步虚机清单 | P1（虚机同步进二期） | 已确认大量虚机/云主机（11.1-13） |
| 事件单管理（V0.5 新增） | 报障->分派->处理->反馈，与告警/巡检联动 | P2 | 无 ITSM，平台内置轻量事件单（11.1-14） |

---

## 5. 功能需求详述

### 5.1 统一工作台（Dashboard）

**目标**：打开平台第一眼看到全局健康状态，不用到处点。

| 功能点 | 说明 | 优先级 |
|---|---|---|
| 全局健康概览 | 设备总数/在线数/告警数（按严重级别）、今日巡检结果 | P0 |
| 分类型统计卡片 | 防火墙/交换机/无线/服务器各自的健康摘要 | P0 |
| 实时告警列表 | 最新告警滚动展示，可点击跳转 | P0 |
| 快捷入口 | 一键巡检、快速搜索设备（按名称/IP/SN/资产编号） | P0 |
| 全局搜索 | 顶部搜索框，输入任意关键字（IP、SN、主机名、位置）模糊匹配定位设备 | P0 |
| 趋势图 | 24h 告警趋势、全网流量趋势 | P1 |
| 工作台自定义 | 卡片可拖拽布局，按角色保存默认视图 | P2 |

### 5.2 系统管理

#### 5.2.1 用户与认证
- 本地账号管理：增删改查、启用/禁用、密码策略（复杂度、有效期）。（P0）
- 登录安全：登录失败锁定、会话超时、可选双因素。（P0）
- 对接飞书统一认证（已确认方向）：飞书开放平台 OAuth2 扫码/SSO 登录，并自动同步组织架构与用户（P1）；一期先以本地账号运行。（原 AD/LDAP 方案取消，以飞书为准）

#### 5.2.2 角色与权限（RBAC）
- 角色管理：预置系统管理员/网络运维/服务器运维/值班/只读等角色，支持自定义。（P0）
- 功能权限：菜单级 + 按钮级（查看/新增/编辑/删除/执行）。（P0）
- 数据权限：按地区、机房、设备类型、设备分组进行数据行级隔离。（V0.4 修正：与 5.2.3 两级站点模型口径统一）（P0）
- 敏感操作二次确认：删除设备、批量执行命令、下发配置需二次确认或审批。（P0）

#### 5.2.3 组织与站点（已确认：地区 -> 机房 两级结构）
- 组织架构：部门树，设备/人员归属部门；对接飞书后自动同步组织架构。（P1）
- 地区管理（一级维度）：按地区/城市划分（如华东、华南、各分公司所在城市），属性：名称、负责人、备注。（P0 数据模型）
- 机房/站点管理（二级维度）：每个地区下可挂多个机房/分支点；机房档案：名称、所属地区、地址、联系人、出口带宽、运营商线路。（P0 数据模型，P1 完整管理界面）
- 全平台（工作台、监控、告警、报表）支持按"地区 -> 机房"两级钻取与汇总视图。

#### 5.2.4 审计日志
- 记录范围：登录日志、操作日志（谁、何时、做了什么、来源IP）、CMDB 数据变更日志（变更前后值 diff）。（P0）
- 审计日志不可删除、不可篡改，保留 ≥ 3 年。（P0）

#### 5.2.5 通知渠道管理（已确认：飞书为主生态）
- 支持飞书（自建应用机器人，主渠道）、邮件（P0）；企业微信、钉钉、Webhook、短信（P2 备选）。
- 飞书消息卡片：告警卡片内嵌"确认 / 处理 / 静默"操作按钮，可 @指定值班人。（P1）
- 渠道连通性测试。（P0）

#### 5.2.6 系统参数
- 采集全局参数（并发数、超时、重试）、数据保留策略、界面偏好等。（P0）

### 5.3 凭据管理（安全基座）

> **这是容易被忽略但必须做在前面**：平台要 SSH/SNMP/API 访问所有设备，凭据的存储安全决定平台自身的安全水位。

| 功能点 | 说明 | 优先级 |
|---|---|---|
| 凭据保险箱 | 设备登录凭据（SSH密码/密钥、SNMP community/v3、API token）AES-256 加密存储，数据库泄露也不可直接读明文 | P0 |
| 凭据分组 | 支持按设备组统一配置（同厂商同密码场景），单台可覆盖 | P0 |
| 访问控制 | 凭据仅采集引擎与授权角色可见，界面默认脱敏显示 | P0 |
| 轮换管理 | 密码到期提醒，配合自动化任务批量改密（P2） | P1/P2 |
| 凭据使用审计 | 记录每次凭据被用于访问哪台设备 | P1 |

### 5.4 机房管理

#### 5.4.1 机房档案
- 机房基础信息：名称、所属地区（一级）、位置、面积、责任人、承重、总电力、出口带宽与运营商。（P1）
- 机房平面示意图（可上传图片标注机柜位置）。（P2）

#### 5.4.2 机柜管理
- 机柜档案：编号、所在机房、行列位置、规格（U数可自定义：42U/47U/50U/其他）、额定电力、额定承重。（P0）
- 机柜列表视图：每柜已用U/总U、电力占比、设备数一览。（P0）

#### 5.4.3 机柜可视化（核心亮点）

**交互需求：**

| 功能点 | 说明 | 优先级 |
|---|---|---|
| 可视化机柜视图 | 以 U 为刻度渲染机柜（1U 一格），从底部 1U 向上编号，与实物一致 | P0 |
| 自由拖拽上架 | 从"待上架设备"列表拖拽设备到机柜指定 U 位，拖动过程实时预览占位 | P0 |
| 设备 U 高识别 | 设备模型携带高度属性（1U 交换机、2U/4U 服务器、ODF 1U、0.5U 配线架等），放置时自动占据连续多 U | P0 |
| 占位调整 | 已上架设备可拖拽换位置/换机柜，自动冲突检测（U 位重叠禁止放置） | P0 |
| U 位状态 | 每格 U 位三种状态：占用（绿色）、空闲（灰色）、预留（黄色，人工标记"留给扩容"） | P0 |
| 空间预警 | 机柜剩余 U < 阈值、电力负载 > 80%、重量超承重时告警提示 | P1 |
| 容量统计面板 | 单柜/全机房视角：已用 U、电力合计（按设备额定功率）、设备数量按类型分布 | P1 |
| 正面/背面视图 | 双面机柜场景（P2） | P2 |
| 点击穿透 | 点击机柜中的设备块，右侧滑出设备详情面板，可跳转 360° 视图 | P0 |
| 健康着色 | 设备块颜色反映实时状态：正常（绿）、告警（黄/红）、离线（灰）——监控与物理位置联动 | P1 |
| 无源设备支持 | 光猫、ODF、PDU、跳线架等同样可上架展示 | P0 |
| 批量初始化 | 支持 Excel 导入初始机柜布局，避免纯手工逐台拖拽 | P1 |
| 3D 机房视图 | 机房级 3D 漫游（P2，锦上添花） | P2 |

**技术实现提示**：基于 SVG/HTML Grid 实现即可（每 U 一个可放置区域），无需引入 3D 引擎；拖拽用原生 HTML5 Drag API 或 vue-draggable；数据结构为 `机柜ID + 起始U + 占用U数`。

#### 5.4.4 线缆管理（与机柜可视化配合）
- 接口级连线记录：设备A的端口 x ↔ 设备B的端口 y，线缆类型（网线/单模光纤/多模光纤/跳纤）、长度、走向。（P1）
- 连线可在设备详情与拓扑图中展示，点击线缆看详情。（P1）
- 线缆布放记录（桥架/管道路由）。（P2）
- 支持与 LLDP 自动发现比对：物理连线记录 vs 实际邻居，不一致时提示（防"台账线缆是错的"）。（P1，强烈推荐）

### 5.5 设备管理（CMDB 核心）

#### 5.5.1 动态模型管理
- **模型 = 设备类型**，内置模型：防火墙、交换机、路由器、无线AC、无线AP、上网行为管理、光猫、ODF、服务器（宿主机）、虚拟机/云主机、PDU、UPS、其他。
- 虚拟化分层建模（V0.5 已确认大量虚机/云主机）：物理服务器（宿主机）与虚机/云主机为两个模型，宿主机 1─N 虚机；虚机清单经 vCenter/云平台 API 定期同步（二期），虚机不占机柜 U 位、通过宿主机间接归属机房。（P1）
- 每个模型由**属性集**构成：内置属性（通用字段）+ **自定义属性**（管理员在界面上加字段，无需改代码）。（P0）
- 属性类型支持：文本、整数、小数、布尔、枚举（可选值管理）、日期、IP、JSON。（P0）
- 模型间关系管理：包含关系（机房⊃机柜⊃设备）、连接关系（接口↔接口）、依赖关系（业务↔设备）。（P0）
- 新增设备类型（如未来加负载均衡、堡垒机）全程界面配置。（P0）

#### 5.5.2 设备台账
- 唯一标识策略：**序列号（SN）为主键**，管理IP、资产编号、主机名作为辅助标识；无源设备（ODF等）用人工编号。（P0）
- 通用内置字段：名称、类型、品牌、型号、SN、管理IP、所在地区/机房/机柜/U位（V0.4 修正：原"站点"口径统一为地区）、责任人、部门、启用日期、保修到期、供应商、生命周期状态（在用/停用/维修/报废/库存）。（P0）
- 占用状态字段（V0.3 评审新增，V0.4 采纳）：空闲/占用中/预约中/锁定维护，与生命周期状态独立（见 5.17.1）；仅对带"测试/研发/共享池"用途标签的设备启用占用机制，生产设备默认"锁定维护"不参与预约。（P1）
- 设备分组：静态分组（手工挑）+ 动态分组（按条件自动匹配，如"H3C核心交换机"）。（P0）
- 列表能力：多条件筛选、自定义列、排序、导出 Excel。（P0）
- 批量导入导出：Excel 模板导入（存量设备初始化利器），导入校验与错误回执。（P0）
- 设备回收站：误删可恢复。（P1）

#### 5.5.3 设备 360° 视图（单设备详情页）
一个页面聚合该设备所有信息，Tab 结构：

| Tab | 内容 | 优先级 |
|---|---|---|
| 概览 | 基础信息 + 实时状态摘要（在线/告警/CPU/内存）+ 所在机柜缩略图 | P0 |
| 监控 | 指标图表（见 5.6 监控中心），接口列表与流量 | P0 |
| 接口 | 接口清单：状态/速率/双工/流量图/错包计数/光功率 | P0 |
| 配置 | 配置备份版本列表与 diff（对接 NCM 模块） | P1 |
| 日志 | 该设备相关 syslog、terminal log | P1 |
| 当前会话 | 正在登录本设备的会话：用户、源IP、登录时间、空闲时长、会话类型（V0.3 评审新增，V0.4 采纳，见 5.17.2） | P1 |
| VLAN/路由 | VLAN 列表、路由表快照及历史变化 | P1 |
| 变更历史 | 台账字段变更记录、配置变更记录、维护记录 | P0 |
| 附件 | 设备照片、合同、保修卡、操作手册 | P1 |

#### 5.5.4 软件状态管理
- 系统版本采集与记录；同型号版本一致性比对（"还有5台交换机没升级"）。（P1）
- VLAN 视图：每台设备的 VLAN 清单、VLAN 全局视图（哪个 VLAN 用在哪些设备）。（P1）
- 路由表快照：定期采集路由表，变更对比（新增/消失路由高亮），路由协议邻居状态（OSPF/BGP邻居）。（P1）
- ARP/MAC 表采集：IP-MAC-端口 映射（IPAM 与安全溯源的底层数据）。（P1）

#### 5.5.5 硬件与接口状态（对接监控中心采集）
- 硬件：CPU、内存、温度、电源模块状态、风扇状态、光模块收发光功率。（P0）
- 接口详情页：
  - 基础：up/down、速率、双工、描述（description）、所属 VLAN；
  - 计数器：入/出流量（bps）、包速率（pps）、总包数、错误包（CRC/align）、丢包、广播/组播包；
  - **增量视图**：错包/丢包按"两次采集的增量/速率"展示（绝对计数器无意义，增量才有意义）；
  - 流量图：1h/6h/24h/7d 可切换；
  - 接口 up/down 历史记录（flapping 检测：频繁翻转子告警）。（P0/P1）

#### 5.5.6 数据质量与自动回填
- 采集到的数据（序列号、型号、版本、接口清单、MAC表）自动回填 CMDB 字段。（P0）
- 字段标记"自动/手工"来源；自动字段被采集覆盖时不覆盖人工锁定字段。（P1）
- 数据质量看板：关键字段缺失设备清单（如无SN、无位置、无责任人）。（P1）

#### 5.5.7 资产生命周期
- 状态机：规划 → 采购中 → 到货入库 → 上架运行 → 维修中 → 备件库 → 报废。（P1）
- 保修到期前 30/60/90 天自动提醒。（P1）
- 合同、采购单、发票附件关联。（P2）

### 5.6 监控中心

#### 5.6.1 采集方式矩阵（按设备类型）

| 设备类型 | 主要采集方式 | 补充采集 | 说明 |
|---|---|---|---|
| FortiGate 防火墙 | SNMP v2c/v3 | REST API（token） | Fortinet 私有 MIB 提供会话数等特有指标 |
| Cisco 防火墙 | SNMP | SSH CLI | 标准Cisco MIB |
| H3C 交换机 | SNMP | SSH CLI（netmiko） | hh3c 私有 MIB：光功率、温度、电源风扇 |
| Cisco WLC 3504（传统AC） | SNMP | SSH CLI | AIRESPACE-* MIB：AP/客户端指标 |
| Cisco 9800-L（多接口新AC） | SNMP | REST API | 9800 新 MIB 体系 + REST；与 3504 分别适配（双驱动） |
| 深信服 AC（上网行为管理） | SNMP | 深信服开放 API | 在线人数、流量排行、认证用户数 |
| 服务器 | **复用现有 Prometheus**（不装新agent） | — | 见 5.6.3 |
| 无源设备 | 无采集 | — | 仅台账 |

#### 5.6.2 采集引擎要求
- 采集调度：周期可配（默认5分钟，接口流量可到1分钟），支持错峰分批，避免同一时刻全部轮询造成网络压力。（P0）
- **大规模采集架构（已确认网络设备 500+ 台）**：采集器支持多实例部署，按地区/机房分片纳管（建议每实例 200~300 台），平台中心统一下发配置、汇聚结果；采集器与平台间通过 Redis 队列同步，Web 层与采集层完全解耦。（P0）
- 健壮性：单设备超时不影响批次，失败重试与退避，连续N次失败判定离线。（P0）
- **离线判定口径统一（V0.4 依据 12.3-3 已定）**：网络设备 = SNMP 连续 3 次采集失败 且 ICMP 探测失败 => 判定离线；服务器 = Prometheus last scrape 超过 2 倍采集间隔 且 ICMP 失败 => 判定采集异常。状态总览统一呈现"在线/离线/采集异常"三态。（P0）
- 厂商驱动适配层：采集逻辑按厂商/平台插件化（类似 Netmiko driver 模式），新增厂商不改核心代码。（P0）
- 指标标准化：所有指标（SNMP采集的也）转成 Prometheus 指标格式存储，**统一用 PromQL 查询**——网络设备与服务器指标一套语言。（P0，架构关键决策）
- SNMP Trap 接收：接收设备主动告警（如端口down、OSPF邻居失效）。（P1）
- 环境温度（已确认：暂不对接第三方动环系统，其 API 封闭）：温度统一取网络设备自带温度传感器；平台预留动环接入接口，未来更换厂商可低成本对接。（P0 传感器 / P2 动环接口预留）
- 采集自监控：采集任务成功率、耗时、排队情况可观测。（P1）

#### 5.6.3 Prometheus 复用方案（服务器监控，重点）

**目标：服务器零新增 agent，平台直接消费现有 Prometheus 数据。**

两种模式（可并存，按需选择）：

| 模式 | 做法 | 适用 | 建议 |
|---|---|---|---|
| A. 直查模式 | 平台通过现有 Prometheus 的 HTTP API 执行 PromQL 查询，结果在平台展示 | 快速上线 | **一期采用**（已确认 300+ 台服务器亦可承受，告警由平台侧周期查询兜底） |
| B. 远写模式 | 现有 Prometheus 配置 remote_write 到平台侧 VictoriaMetrics，数据统一落库 | 统一存储、统一告警、长期保留 | **二期落地**（已确认 300+ 台，此为最终形态，建议尽早切换） |

配套需求：
- CI 自动关联：通过 instance/IP 标签自动匹配 CMDB 设备，服务器自动"挂"到台账与机柜位。（P0）
- 指标映射配置：node_exporter 常用指标（CPU/内存/磁盘/网络/负载）开箱即用展示。（P0）
- 服务器离线检测：last scrape 时间超阈值判定采集失败。（P0）
- 虚机/云主机指标（V0.5）：已装 node_exporter 的虚机直接复用；未装的经 vCenter/云监控 API 采集后统一转 VictoriaMetrics。（P1，二期）

#### 5.6.4 各设备类型核心指标清单

**防火墙（FortiGate / Cisco）**

| 指标 | 说明 | 优先级 |
|---|---|---|
| CPU / 内存使用率 | | P0 |
| 并发会话数 / 新建会话速率 | 防火墙核心容量指标 | P0 |
| 接口状态与流量 | 含 zone 接口 | P0 |
| HA 状态（主备） | 主备切换事件告警 | P0 |
| IPsec/SSL VPN 隧道状态与流量 | | P1 |
| 策略命中计数（TOP N） | 策略优化依据：长期0命中策略 | P2 |
| 温度 | | P1 |

**H3C 交换机**

| 指标 | 说明 | 优先级 |
|---|---|---|
| CPU / 内存（含五秒/一分钟采样） | | P0 |
| 接口：状态/速率/双工/流量/错包/丢包/广播包 | 增量速率展示 | P0 |
| 光模块收发光功率 | 光衰预警（低于阈值提前换模块） | P1 |
| 电源模块/风扇状态 | | P0 |
| 温度 | | P0 |
| PoE 功率（若下挂AP/摄像头） | | P1 |
| MAC 地址表规模 | | P2 |

**Cisco WLC（无线AC，已确认 9800-L 与 3504 混合部署，双驱动适配）**

| 指标 | 说明 | 优先级 |
|---|---|---|
| 在线 AP 数 / 离线 AP 列表 | | P0 |
| 关联客户端总数 | | P0 |
| CPU / 内存 | | P0 |
| AP 明细同步：名称/IP/信道/发射功率/客户端数/上联交换机端口 | 从 AC 自动同步 AP 台账到 CMDB | P1 |
| Rogue AP 检测数量 | | P2 |
| 高信道利用率/干扰 AP 列表 | 无线体验优化 | P2 |

> 采集适配说明（已确认双型号）：3504 走 AIRESPACE-\* MIB；9800-L 走新 MIB 体系 + REST API。平台内置两个 WLC 驱动，按设备型号自动选择；AP 台账同步逻辑两代保持一致。

**上网行为管理（深信服 AC，已确认）**

| 指标 | 说明 | 优先级 |
|---|---|---|
| 在线用户数 / 认证用户数 | | P0 |
| 总出口流量（分上下行） | | P0 |
| TOP 应用 / TOP 用户流量排行 | | P1 |
| CPU / 内存 | | P0 |

**服务器（复用 Prometheus）**

| 指标 | 来源 | 优先级 |
|---|---|---|
| CPU / 内存 / 磁盘使用率与IO / 网卡流量 / 系统负载 | node_exporter | P0 |
| 磁盘 SMART 健康（若有） | smartmontools exporter | P2 |
| 进程/端口存活 | process/blackbox exporter（若已部署） | P1 |

#### 5.6.5 状态总览
- 全网设备在线率大盘（分类型/分地区/分机房）。（P0）
- 实时 TopN：CPU TOP10、接口流量 TOP10、错包增量 TOP10。（P1）
- 指标视图支持自定义时间范围、对比（今天 vs 上周同期）。（P1）
- 图表下钻：大盘 → 设备 → 接口。（P1）

### 5.7 告警中心（原需求未覆盖，必做）

| 功能点 | 说明 | 优先级 |
|---|---|---|
| 告警规则引擎 | 指标阈值（CPU>80%持续5分钟）、状态类（设备离线、接口down、电源故障、HA切换、OSPF邻居down）、日志关键字（对接日志中心） | P0 |
| 告警分级 | 紧急/重要/一般/提示，不同颜色与通知策略 | P0 |
| 通知路由 | 按设备分组/类型/时间段路由到不同渠道与值班人 | P0 |
| 告警收敛 | 同设备短时间多指标异常合并为一条事件（防告警风暴）；根因抑制（上游设备故障时抑制下游衍生告警，需拓扑数据支持） | P1 |
| 静默/维护窗口 | 计划内变更期间静默，自动恢复；复用同一机制实现"被占用测试设备"告警降噪（占用即静默、释放即恢复，见 5.17.1，V0.4 采纳不另建机制） | P0 |
| 告警闭环 | 告警状态机：触发→通知→确认（认领）→处理中→已解决→关闭；处理记录留痕 | P0 |
| 告警升级 | 超时未确认自动升级通知上级 | P1 |
| 告警与拓扑联动 | 拓扑图上设备图标实时变色 | P1 |
| 历史与统计 | 告警趋势、TOP告警设备、MTTA/MTTR 统计 | P1 |

### 5.8 巡检中心

**目标：把每天人肉登设备敲命令的巡检，变成平台定时任务 + 自动报告。**

#### 5.8.1 巡检模板
- 检查项可配置，类型支持：
  - **阈值类**：指标超过阈值判异常（CPU > 80%）；
  - **状态类**：期望值比对（电源状态=正常、HA=master/slave正常、接口=up）；
  - **脚本类**：自定义命令输出与断言（执行 `display cpu-usage` 检查关键字）；
  - **组合类**：多个检查项加权计算健康分。（P0）
- 预置模板：
  - 网络设备通用日巡检：CPU/内存/温度/电源风扇/接口错包增量/离线设备；
  - 防火墙专项：会话数水位、HA状态、VPN隧道；
  - 无线专项：离线AP、客户端数突变；
  - 服务器专项：基于 Prometheus 指标（磁盘/CPU/内存）。（P0）

#### 5.8.2 巡检任务
- 定时调度：cron 表达式（每日 09:00 巡检、每周深度巡检）。（P0）
- 巡检范围：指定设备分组/机房/全部。（P0）
- 手动一键巡检：临时全量/局部巡检。（P0）
- 失败重试与超时控制。（P1）

#### 5.8.3 巡检报告
- 自动生成报告：整体健康分、异常设备清单、异常明细（指标实际值 vs 阈值）、与上次巡检对比。（P0）
- 报告自动推送到飞书/邮件。（P0）
- 报告导出：PDF / Word / Excel。（P0）
- 报告归档与检索（按日期/任务查询历史报告）。（P1）
- 异常项自动生成告警，进入告警闭环；**与监控告警共用同一告警事件表，去重键 = 设备 + 检查项/规则**，避免巡检异常与监控告警重复通知（V0.4 采纳 12.2-14）。（P0）
- AI 巡检解读：大模型对报告生成摘要、趋势分析与整改建议（见 5.13）。（P2）

### 5.9 拓扑管理

#### 5.9.1 拓扑数据来源
- **自动发现（核心）**：通过 LLDP（H3C）/CDP（Cisco）读取邻居表，自动构建设备间连接关系与 L2 拓扑；AP 与 WLC 关系从 AC 同步。（P1）
- ARP/路由表辅助推断 L3 关系。（P2）
- 手工修正：自动发现结果上手工增加/删除/修正连线。（P1）

#### 5.9.2 拓扑展示与编辑
- 交互式画布：缩放、拖拽、框选、自动布局（分层布局：出口→核心→汇聚→接入→终端）。（P1）
- 分层视图：按核心/汇聚/接入分层呈现；可按机房/业务系统筛选子图。（P1）
- 多拓扑类型：物理拓扑（LLDP）、三层拓扑（路由邻居）、无线拓扑（AC-AP）、自定义业务视图。（P1/P2）
- 节点展示设备图标（按类型）、名称、IP；可配置展示项。（P1）
- 连线展示接口名与实时带宽利用率（粗细/颜色）。（P1）
- 告警联动：设备告警时节点变色闪烁，可快速定位影响面（"这台核心交换机故障影响下挂37台AP"）。（P1）
- 拓扑导出图片。（P2）

### 5.10 日志中心

| 功能点 | 说明 | 优先级 |
|---|---|---|
| Syslog 接收 | 平台作为 syslog server（UDP/TCP 514），网络设备日志统一外发至此；按设备/级别/时间存储 | P1 |
| 设备日志抓取 | 对不便外发 syslog 的设备，定期 CLI 登录抓取 logbuffer | P1 |
| Terminal 操作日志 | 通过平台对设备执行命令的全过程记录（谁、何时、执行了什么命令、回显），作为运维审计 | P1 |
| 堡垒机集成 | 已确认公司使用 JumpServer（V0.5）：通过其 API 对接会话记录与登录审计，作为 5.17.2/5.17.3 的服务器侧数据源 | P1 |
| 日志检索 | 按设备、时间范围、级别（emerg~debug）、关键词组合查询 | P1 |
| 关键日志告警 | 正则规则：如 `OSPF.*down`、`LINK.*DOWN`、`login failed` 触发告警 | P1 |
| 日志保留策略 | 原始日志 ≥ 90 天，可按存储扩展 | P1 |
| 日志采集健康度 | 各设备日志最近到达时间监控（防"设备日志没发出来却没人知道"） | P1 |

### 5.11 网络配置管理 NCM（强烈建议补充的模块）

> 网络运维三大刚需之一：配置备份、变更对比、变更留痕。出问题时第一个问题永远是"谁改了什么"。

| 功能点 | 说明 | 优先级 |
|---|---|---|
| 定时配置备份 | 每日自动 SSH/API 登录设备备份 running-config / startup-config（FortiGate 全配置、H3C cfg、Cisco run） | P1 |
| 事件触发备份 | 检测到配置变更（syslog 或定期 diff）立即备份新版本 | P1 |
| 版本库 | 每设备多版本留存，含备份时间、触发方式；支持一键回看任意版本 | P1 |
| 配置 diff | 任两版本对比，变更行高亮（新增绿/删除红）；支持与"上次版本"快速对比 | P1 |
| 变更事件记录 | 配置变更时间轴：何时变的、变了哪几行、关联的 syslog 事件 | P1 |
| 敏感信息处理 | 备份文件中的密码字段加密存储，查看需权限 | P1 |
| 批量下发/预配置 | 配置片段批量下发（需审批流+灰度执行） | P2 |
| 安全基线核查 | 规则库：如"必须配置 loghost/NTP/ACL""VTY 必须限制源IP"，巡检联动输出不合规清单 | P2 |

### 5.12 自动化运维

> 高危能力，权限与审批是第一设计约束。

| 功能点 | 说明 | 优先级 |
|---|---|---|
| 命令/脚本库 | 常用操作模板化：保存配置、重启端口、批量改描述、批量关端口… | P2 |
| 批量执行 | 按设备组批量下发命令，逐台回显汇总，失败清单 | P2 |
| 灰度执行 | 先执行 1 台 → 人工确认 → 再执行剩余 | P2 |
| 审批流 | 高危脚本执行需主管审批（对接通知渠道审批卡片） | P2 |
| 变更窗口 | 变更计划登记，联动告警静默 | P2 |
| 任务编排 | 多步骤流程编排（如"新交换机上线标准流程"：备份→改配置→验证→再备份） | P2 |
| 操作全程审计 | 所有自动化执行留痕（执行人、时间、目标、输出） | P2 |
| Ansible 集成 | 复用 Ansible playbook 生态 | P2 |

### 5.13 AI 智能运维（差异化亮点）

#### 5.13.1 AI 基础设施
- LLM 网关（已确认：对接公司自建 newapi 反代，OpenAI 兼容端点）：LLM 网关仅需实现 OpenAI 兼容协议客户端（可配置 base_url / api_key / model），模型选择与路由由 newapi 统一管理；平台侧负责密钥保管、按场景的模型参数映射（model/temperature）、用量统计与成本看板。（P2，一期预留接口）
- Prompt 管理与评测：核心场景 prompt 模板化管理。（P2）

#### 5.13.2 落地场景（按价值排序）

| 场景 | 说明 | 优先级 |
|---|---|---|
| 自然语言查 CMDB（NL2Query） | "帮我找出CPU超过80%的H3C交换机"、"保修明年到期的设备有哪些"→ 自动转查询语句返回结果表格 | P2 |
| 告警根因分析 | 告警触发时自动聚合：设备指标 + 拓扑邻居状态 + 近1小时日志 + 近期配置变更 + 最近巡检结果 → 生成根因假设与排查建议（RAG 结合历史案例） | P2 |
| 巡检报告智能解读 | 巡检报告自动生成管理层摘要：本周健康趋势、TOP风险、整改建议 | P2 |
| ChatOps 值班助手 | 飞书机器人（主渠道，已确认）：值班人员直接在飞书里查设备、看告警、触发巡检；架构上支持扩展企微/钉钉渠道 | P2 |
| 运维知识库 RAG | 历史故障案例、操作手册入库；"交换机这个报错什么意思"直接给答案并引用出处 | P2 |
| 配置变更风险评审 | 提交配置变更时，AI 分析 diff 的风险点（"该变更可能导致OSPF邻居中断"） | P2 |
| 指标异常检测 | 动态基线（而非固定阈值）：学习接口流量日常模式，节假日流量异常波动识别。数据基础结论（V0.4 答复 12.3-5）：90 天原始 + 1 年 5 分钟聚合足以支撑 v1 动态基线（需 ≥30 天训练数据）；跨年度季节性对比需求出现时再延长保留 | P2 |
| 周报/月报自动生成 | 汇总告警、变更、巡检、容量数据自动成文 | P2 |
| 设备健康评分 | 多指标加权（CPU/内存/错包/年限/告警频次）计算健康分，指导更换计划 | P2 |

#### 5.13.3 AI 安全护栏（必须）
- AI 全部输出标注"仅供参考"，变更类建议必须人工确认后才可执行（AI 不直接操作生产设备，一期严格只读）。
- 传给 LLM 的数据脱敏（IP/密码/拓扑细节可配置过滤）。注：newapi 为公司自建反代，数据不出公司边界，脱敏策略可适度放宽，但保留开关。
- 关键决策（根因结论、风险结论）展示引用的证据链（哪条日志/哪个指标），可溯源。

### 5.14 IPAM（IP 地址管理）

| 功能点 | 说明 | 优先级 |
|---|---|---|
| 网段管理 | VLAN ↔ 网段（如 VLAN10 = 10.1.10.0/24）登记，网关、用途、责任人 | P1 |
| IP 台账 | IP 分配记录：IP ↔ 设备/MAC ↔ 端口 ↔ 使用人 | P1 |
| 自动发现占用 | 从交换机 ARP/MAC 表自动学习 IP 实际占用，与登记冲突标红（"登记给A但B在用"） | P1 |
| 可视化 | 网段格子图：空闲/已用/冲突/保留 | P1 |
| DHCP 集成 | 对接 DHCP lease 数据 | P2 |

### 5.15 报表中心

| 报表 | 内容 | 优先级 |
|---|---|---|
| 资产报表 | 按类型/厂商/机房/状态统计；保修到期清单 | P1 |
| 容量报表 | 机柜空间与电力、接口利用率、光模块光衰趋势 | P1 |
| 告警运维报表 | 告警趋势、TOP告警设备、MTTA/MTTR、告警处理率 | P1 |
| 巡检月报 | 健康分趋势、遗留问题清单 | P1 |
| 变更报表 | 配置变更统计、自动化执行统计 | P2 |
| 自定义报表 | 按需配置字段与图表，定时邮件发送 | P2 |

### 5.16 开放平台
- REST API：覆盖 CMDB 增删改查、指标查询、告警查询；Swagger 文档自动生成。（P0 提供 CMDB 基础 API，后续全量）
- API Token 管理：应用级密钥、权限范围、限流。（P1）
- AI 只读 Token（V0.4 依据 12.3-6 已定）：一期即为 AI 场景提供独立只读 Token，限定 CMDB/指标/告警查询端点，默认限流 60 次/分钟，独立审计调用记录。（P0）
- Webhook 事件推送：设备上下线、告警、配置变更事件推给外部系统。（P2）

### 5.17 设备使用与共享（V0.3 评审补充；V0.4 方案作者 Review 通过）

> **评审补充**：原需求聚焦"设备健康、位置、配置"，未覆盖"设备当前是否有人使用、是否空闲、能否被他人复用"。为支撑测试机/共享设备的高效复用补充本模块，属资源管理范畴。
>
> **V0.4 Review 结论**：方向正确，整体采纳，P1 维持二期。三点边界修正：① 占用/预约机制仅对带"测试/研发/共享池"用途标签的设备启用，生产网络设备不参与（避免"核心交换机被预约"的语义混乱）；② 占用期告警降噪复用 5.7 静默机制，不另建；③ 服务器会话采集与"零新增 agent"原则（5.6.3）冲突，按显式例外处理：优先复用堡垒机会话数据，无堡垒机则仅为共享池测试机部署轻量会话上报组件（见 5.17.2）。

#### 5.17.1 占用状态管理（动态占用，区别于生命周期状态）

- 设备两个独立状态字段（避免语义混淆）：
  - **生命周期状态**（原 5.5.2 保留）：在用/停用/维修/报废/库存——表示资产所处阶段；
  - **占用状态（新增）**：`空闲 / 占用中 / 预约中 / 锁定维护`——表示当前是否有人在使用。
- 占用记录（DeviceUsage）：设备、占用用户、占用类型（预约/占用/历史）、开始时间、计划结束、实际释放、用途（项目/任务）、关联审批单/工单号。
- 操作与规则：预约（同一时间窗口冲突检测，仅一人）、占用、释放（校验本人/管理员）、超时未释放提醒、管理员强制释放。
- 占用期间告警降噪：被占用测试机的噪音告警可标记"已知占用"并抑制，避免误扰。
- 与机柜可视化（健康着色）、设备 360° 视图联动展示占用状态。

#### 5.17.2 登录状态与当前会话监测

- **关键区分**：设备"在线/离线"（网络可达性）≠"当前有人正在登录操作"。原文档口径只有前者（见 5.6.2/5.6.3）。
- 采集来源：
  - 网络设备：定时 CLI `display users` / `show users`，或 SNMP 采集活跃 VTY/Console 会话；
  - 服务器：`who` / `last` / utmp（node_exporter 不采会话，需轻量 agent 或对接堡垒机）；
  - 防火墙/WLC：WebUI 登录、API token 调用记录。
- 数据源决策（V0.4 Review；V0.5 已确认公司有 JumpServer）：网络设备会话采集复用现有 SSH 采集器（低频 5~10 分钟，`display users`/`show users` 走既有 netmiko 驱动，零新增组件）；服务器侧统一对接 JumpServer API 获取会话与登录审计（见 7.3），无需部署任何新组件。
- 设备 360° 视图新增"当前会话"Tab：远程用户、**远程源 IP**、登录时间、会话时长、空闲时长、会话类型（SSH/Console/Web/API）。

#### 5.17.3 远程登录审计（Login Event）

- 记录"谁 + 从哪个源 IP + 何时 + 登了哪台 + 成功/失败"，覆盖**绕开平台直接 SSH/RDP/Console 登录**的行为（原 5.10 Terminal 操作日志仅覆盖经平台执行的命令）。
- 多源合并：设备 Syslog 登录事件、CLI 抓取 login/last、服务器 secure/wtmp、堡垒机会话、平台自身执行记录 → 统一为 LoginEvent 表。
- 检索与报表：按用户 / 源 IP / 设备 / 时间范围查询"谁碰过这台机器"。
- 异常检测（P2）：同账号多源 IP、源 IP 非内网段、非工作时间登录、暴力破解尝试（与附录 B "login failed" 巡检项联动）。

#### 5.17.4 系统资源统计与"空闲且可用"检索

- 在物理容量统计（机柜 U / 额定电力 / 承重 / 接口利用率，见 5.4.3）之外，补充**逻辑资源口径**：
  - 服务器/测试机：CPU 核数、内存总量/可用、磁盘可用、GPU（若有）、当前负载；
  - 交换机：空闲端口数、PoE 剩余功率；
  - IPAM：空闲 IP 数。
- **可用资源检索**：组合条件（占用状态=空闲 + 在线 + CPU<50% + 内存可用≥8G + 有GPU 等）一次列出"现在就能用的设备"——需打通"监控指标 ↔ CMDB 台账 ↔ 占用状态"三方联合查询。
- 共享资源池页面：空闲池 / 占用池 / 预约池，普通员工可查可申请（受数据权限约束）。

#### 5.17.5 共享与使用标签

- 设备"用途标签"：生产 / 测试 / 研发 / 共享池；"可共享"开关（仅限本人 vs 允许他人申请）。
- 与报表中心（5.15）联动：设备利用率（占用时长占比）、共享池周转报表。
- 数据模型见附录 A（DeviceUsage / LoginEvent）。

---

## 6. 非功能需求

| 维度 | 要求 |
|---|---|
| 规模容量 | 已确认规模：网络设备 500+ 台、服务器 300+ 台；设计目标：网络设备 ≥ 1000 台、服务器 ≥ 1000 台、监控指标 ≥ 30 万活跃序列；采集器按地区/机房分片、支持多实例水平扩展，无架构硬上限 |
| 采集性能 | 单采集实例一轮 200~300 台设备 SNMP 采集 ≤ 2 分钟（并发可配）；指标写入延迟 ≤ 30 秒 |
| 页面性能 | 常规页面首屏 ≤ 2 秒；机柜视图 50 柜同屏不卡顿 |
| 可用性 | 平台自身 99.9%；容灾目标 **RPO ≤ 24h / RTO ≤ 4h**（每日全量备份 + 配置备份异地副本），上线后每半年演练一次恢复；采集器容灾：单采集器故障时由中心或其他实例降频接管其分片（V0.4 采纳 12.2-17/18） |
| 时区 | **UTC 存储 + 前端按用户时区本地化展示**（多地区多机房场景，一期定死，迟改成本极高）（V0.4 采纳 12.2-15） |
| 告警 SLA | 紧急：5 分钟确认 / 30 分钟响应；重要：15 分钟确认 / 2 小时响应；一般：1 个工作日。目标值可配置，超时自动触发 5.7 告警升级（V0.4 采纳 12.2-16） |
| 数据保留 | 原始指标 90 天、5分钟聚合 1 年、告警与审计 ≥ 3 年、配置备份永久 |
| 安全 | 全站 HTTPS；凭据 AES-256 加密；密码不明文出现在任何 API/日志；RBAC + 数据权限；操作审计不可篡改 |
| 兼容 | Chrome/Edge 现代浏览器；SNMP v1/v2c/v3；SSH2 |
| 可扩展 | 新设备类型、新厂商驱动、新告警渠道均为插件式扩展，不改核心 |
| 部署 | 已确认：虚机/物理机 + Docker Compose 一键部署；采集器可按地区独立部署为第二套 Compose 栈；保留平滑迁移 K8s 的可能 |

---

## 7. 技术架构建议（供研发评审，非强制）

### 7.1 参考架构

```
┌──────────────────────────────────────────────────────────┐
│  前端 SPA：Vue3 + TypeScript + Element Plus               │
│  机柜可视化：SVG Grid + HTML5 Drag                        │
│  拓扑画布：AntV G6 / X6                                    │
├──────────────────────────────────────────────────────────┤
│  API 层：Django 5 + DRF（模块化 App 设计，见 7.2.1）        │
│  ├─ CMDB 服务   ├─ 监控服务   ├─ 告警服务                  │
│  ├─ 巡检服务    ├─ 拓扑服务   ├─ NCM 服务                  │
│  └─ AI 网关（LLM 适配层）                                  │
├──────────────────────────────────────────────────────────┤
│  采集引擎（独立进程/容器，水平扩展）                        │
│  ├─ SNMP 采集器（pysnmp/easysnmp，多进程并发）             │
│  ├─ SSH 采集器（netmiko —— 内置各厂商驱动）                │
│  ├─ API 采集器（FortiGate REST / WLC REST）               │
│  ├─ Syslog Receiver（UDP/TCP 514）                        │
│  └─ Prometheus 对接器（HTTP API 查询 / remote_write）      │
├──────────────────────────────────────────────────────────┤
│  存储：PostgreSQL（CMDB主数据+JSONB动态属性）              │
│        VictoriaMetrics（指标，兼容PromQL，压缩比高）        │
│        日志：起步用 PostgreSQL/ClickHouse，量大转 ES/Loki  │
│        Redis（任务队列+缓存）                              │
├──────────────────────────────────────────────────────────┤
│  调度：APScheduler / Celery（巡检、备份、定时任务）        │
│  部署：Docker Compose → K8s                               │
└──────────────────────────────────────────────────────────┘
```

### 7.2 关键技术决策建议

| 决策点 | 建议 | 理由 |
|---|---|---|
| 后端框架 | Django 5 + DRF + Celery（已确认，模块化建设，见 7.2.1） | Django Admin 可作实施期数据修正利器；ORM/迁移成熟；Celery 天然适配巡检/备份调度；netmiko/pysnmp 等采集库同为 Python 生态，采集层可无缝复用 |
| 动态模型存储 | PostgreSQL JSONB | 自定义属性免 DDL 变更；配合 GIN 索引保证查询性能 |
| 指标统一 | 一律转 Prometheus 指标格式存 VictoriaMetrics | 网络设备(SNMP)与服务器(Prometheus)同一套查询语言，Grafana 可直接复用，AI 查询也统一 |
| 拓扑画布 | AntV G6 | 图布局/交互开箱即用 |
| 时序库 | VictoriaMetrics 而非自建 Prometheus 存储 | 单机资源占用低、压缩比高、兼容 PromQL、天然支持 remote_write 对接现有 Prometheus |
| 配置备份 | 参考 Oxidized 设计（可先直接用 Oxidized 顶一期） | 成熟方案，快速见效 |
| 采集架构 | 采集器独立进程，按地区/机房分片、多实例水平扩展 | 已确认 500+ 台网络设备；单实例 200~300 台压力可控且扩展自如 |
| LLM 接入 | OpenAI 兼容协议对接公司自建 newapi | 已确认：模型路由与配额由 newapi 统一管理，平台不绑定单一模型供应商 |

#### 7.2.1 Django 模块化设计原则（已确认：Django 框架 + 模块化建设）

- **模块 = 独立 Django App**：`apps/system`（系统管理）、`apps/cmdb`（模型/台账）、`apps/dcim`（机房/机柜/线缆）、`apps/monitor`（采集与指标）、`apps/alert`、`apps/inspect`（巡检）、`apps/topo`、`apps/ncm`（配置管理）、`apps/automate`（自动化）、`apps/ipam`、`apps/ai`、`apps/report`、`apps/usage`（设备使用与共享，V0.3 评审新增）、`apps/change`（轻量变更单，V0.4 采纳 12.2-5）。
- **边界纪律**：App 之间禁止直接 import 对方内部模型，一律通过对外 Service 接口或 Django signal 交互；每个 App 可独立启停（INSTALLED_APPS 增删即插拔）。
- **采集引擎不进 Web 请求路径**：采集器为独立进程（Celery worker / 独立容器），与 Web 层通过 Redis 队列 + 数据库解耦，500+ 台设备的采集压力不影响页面响应。
- **统一插件规范（registry 模式）**：设备厂商驱动、告警渠道、通知渠道、巡检检查项类型均实现"注册器 + 插件类"，新增厂商/渠道零改核心代码。
- **前后端分离**：Django 仅提供 REST API（DRF）+ 认证（Session/Token/OAuth 飞书）；前端 Vue3 独立部署；Django Admin 保留作为实施期批量修数辅助工具。
- **基础设施复用 Django 生态**：Celery（巡检/备份/定时任务调度）、django-redis、drf-spectacular（OpenAPI 文档自动生成，即 5.16 开放平台的文档底座）。

### 7.3 外部系统对接清单

| 系统 | 对接方式 | 用途 |
|---|---|---|
| 现有 Prometheus | HTTP API（一期）/ remote_write（二期） | 服务器指标复用（已确认 300+ 台） |
| 飞书开放平台 | OAuth2/SSO + 自建应用机器人 + 消息卡片 | 统一认证（P1）+ 告警/审批通知主渠道 + ChatOps（已确认主 IM 生态） |
| 企业微信/钉钉 | 群机器人 Webhook / 应用 | 备用通知渠道（P2） |
| JumpServer 堡垒机（V0.5 已确认在用） | JumpServer API（会话记录/登录审计/命令回放） | 5.17.2 当前会话、5.17.3 LoginEvent 的服务器侧数据源（P1） |
| 公司自建 newapi | OpenAI 兼容 API（可配 base_url/api_key/model） | AI 能力统一入口（已确认，P2） |

---

## 8. 竞品与开源参考

| 项目 | 可借鉴点 |
|---|---|
| NetBox | 网络设备 CMDB 的行业标杆：数据模型（站点/机柜/设备/接口/线缆/IPAM）、机柜可视化（Rack Elevation）——**重点研究其数据模型设计** |
| 蓝鲸 bk-cmdb + 蓝鲸监控 | 动态模型、自动发现、作业平台的整体思路 |
| Oxidized | 网络设备配置备份（150+ 厂商驱动） |
| LibreNMS / Zabbix | SNMP 采集、设备自动发现、告警规则设计 |
| JumpServer | 终端会话审计思路 |
| Grafana | 指标看板（可直接嵌套复用） |

> 决策结论（已确认）：**全自研**。数据模型与机柜可视化交互参考 NetBox 的设计，不基于其代码二开。

---

## 9. 实施路线图（建议）

### 一期：底座与可视化（约 6~8 周）
**主题：台账准起来，状态看起来**

- 前置任务：批量开启剩余设备 SNMP（已确认当前部分开启），统一 v2c/v3 策略并入库凭据
- 系统管理（用户/角色/RBAC/审计）；通知渠道一期落地：飞书 + 邮件
- 凭据管理
- 机房/机柜管理 + 机柜可视化拖拽上架（含"地区 -> 机房"两级站点模型）
- CMDB 核心（动态模型、台账、Excel 导入、360°视图框架）
- 监控中心基础版：SNMP 采集（FortiGate/Cisco 防火墙、H3C 交换机、深信服 AC、3504 与 9800-L 双 WLC 驱动）、Prometheus 直查复用、状态总览
- 告警中心基础版：阈值/离线规则、飞书/邮件通知、告警闭环
- 巡检中心基础版：通用巡检模板、定时任务、自动报告推送
- 开放 API：CMDB 基础 CRUD
- 数据埋点（评审补充）：设备"占用状态"字段、登录事件（LoginEvent）表结构、可共享标记（为二期"设备使用与共享"提前铺数据，见 5.17）

**验收**：全部存量设备录入且位置可视；网络设备 SNMP 全量开启并纳入采集；CPU/内存/接口/离线可看；每日巡检报告自动推送飞书/邮件。

### 二期：自动化数据与网络专项（约 6~8 周）
**主题：数据自动流动，网络管理闭环**

- Syslog 接收 + 日志检索 + 关键日志告警
- NCM 配置备份、版本 diff、变更事件
- 拓扑：LLDP/CDP 自动发现 + 手工编辑 + 告警联动
- 接口光功率、电源风扇、环境温度专项
- VLAN/路由快照、ARP/MAC 采集
- AP 台账自动同步（WLC）
- 告警收敛/抑制、维护窗口
- IPAM 基础版
- 飞书统一认证：SSO 扫码登录 + 组织架构/用户自动同步
- Prometheus remote_write 切换：指标统一落库平台侧 VictoriaMetrics（300+ 台服务器的最终形态）
- 虚机/云主机纳管（V0.5 新增）：虚机模型 + vCenter/云平台 API 同步虚机清单、宿主机关联（已确认大量虚机，见 11.1-13）
- 设备使用与共享模块（评审补充）：占用/预约/释放与冲突检测、登录会话监测、远程登录审计、可用资源检索、共享资源池（见 5.17）
- 交换机空闲端口容量统计与"新设备能接哪"检索（V0.4 采纳 12.2-9：接口清单已采集，仅需统计口径，提前至二期）
- 轻量变更单（V0.4 采纳 12.2-5）：申请->审批->实施->验证->关闭，与 NCM 变更留痕、自动化执行打通，不做重型 ITSM

**验收**：配置变更 100% 留痕可 diff；拓扑自动生成并联动告警；AP 台账与 AC 自动一致；共享测试机"空闲可查、占用可约、登录可审计"（评审补充项）。

### 三期：效率与资产（约 4~6 周）
**主题：少登设备，资产清楚**

- 自动化运维：脚本库、批量执行（灰度+审批）
- 轻量事件单（V0.5 已定，无 ITSM 故平台内置）：报障->分派->处理->反馈，与告警/巡检异常联动，见 11.1-14
- 安全基线核查
- 资产生命周期 + 保修提醒；扩展：设备借用/外借登记、License/维保/服务合同管理（V0.4 采纳 12.2-1/3，并入本模块不单拆）
- 报表中心
- 线缆管理与 LLDP 比对
- remote_write 统一指标存储
- 固件升级作业计划（批次/窗口/回滚，V0.4 采纳 12.2-4）与值班排班/交接班（V0.4 采纳 12.2-7，联动告警升级）
- PDU 实际功耗采集与远程电源控制（V0.4 采纳 12.2-10/12；前提为智能 PDU 硬件，远程断电属高危操作需强审批）

### 四期：AI 智能运维（约 6 周+，持续迭代）
**主题：让 AI 干活**

- LLM 网关 + NL2Query
- 巡检报告 AI 解读
- 告警根因分析（拓扑+日志+变更关联）
- ChatOps 值班助手
- 知识库 RAG + Runbook 运维手册管理（V0.4 采纳 12.2-8：Runbook 与 RAG 同源建设，Runbook 即 RAG 语料）
- 容量预测（V0.4 采纳 12.2-11：机柜/带宽/端口打满时间预测，依赖 ≥6 个月数据积累）
- 异常检测与设备健康评分

---

## 10. 风险与应对

| 风险 | 影响 | 应对 |
|---|---|---|
| 存量设备初始化工作量巨大 | 一期上线慢 | Excel 批量导入 + SNMP 自动发现回填双管齐下；先核心设备后长尾 |
| SNMP 未全量开启（已确认部分开启） | 采集覆盖不全 | 列入一期前置任务：按厂商分组批量下发配置，先在测试设备验证再全网推广 |
| Django 单体随模块增多变得笨重 | 迭代变慢 | 严格执行 App 模块化边界（7.2.1）；采集等重负载进程独立部署，Web 层保持轻 |
| 厂商 MIB/CLI 差异导致适配工作量超预期 | 采集覆盖不全 | 驱动插件化；优先适配在用型号；标准 MIB 打底 |
| SNMP 大规模轮询对网络造成压力 | 业务影响 | 错峰分批、并发限流、采集频率分级 |
| 台账数据"录入即过时" | CMDB 失去公信力 | 自动发现回填为主，人工录入为辅；数据质量看板督促 |
| 自动化操作误伤生产 | 重大事故 | 灰度执行、审批流、变更窗口、高危命令黑名单、全程审计 |
| AI 幻觉给出错误结论 | 误导处置 | 证据链引用、只读起步、人工确认门禁 |
| 平台自身安全（存了所有设备密码） | 被攻破等于全网沦陷 | 凭据加密、最小权限、平台部署于管理网段、操作审计 |
| 单人维护平台（知识孤岛） | 人员离职风险 | 文档化、代码规范、部署脚本化 |

---

## 11. 需求决策记录（V0.2 已确认）与遗留问题

### 11.1 已确认决策（15 项）

| # | 决策项 | 结论 | 对设计的影响 |
|---|---|---|---|
| 1 | 网络设备规模 | **500 台以上** | 采集器多实例分片架构（按地区/机房），一期即按水平扩展设计（见 5.6.2） |
| 2 | 服务器规模 | **300 台以上** | 一期 Prometheus 直查，二期切 remote_write 统一存储（见 5.6.3） |
| 3 | 部署环境 | **虚机/物理机 + Docker Compose** | 一键部署；采集器可按地区独立成栈 |
| 4 | 后端技术栈 | **Django 框架，模块化建设** | Django 5 + DRF + Celery；模块=独立 App，边界纪律见 7.2.1 |
| 5 | 建设路线 | **全自研** | 数据模型/机柜可视化参考 NetBox 设计，不基于其二开 |
| 6 | 大模型接入 | **公司自建 newapi 反代（OpenAI 兼容端点）** | LLM 网关实现 OpenAI 兼容客户端（可配 base_url/key/model），模型路由由 newapi 统一管理 |
| 7 | 认证/IM 生态 | **飞书** | 飞书 OAuth 统一认证（二期）；通知/审批/ChatOps 主渠道均为飞书 |
| 8 | 上网行为管理 | **深信服 AC** | SNMP 为主 + 深信服开放 API 适配 |
| 9 | 站点结构 | **地区（一级）-> 机房/分支（二级），多地区多机房** | 站点模型两级化，全平台按地区/机房两级钻取汇总（见 5.2.3） |
| 10 | 动环监控 | **有动环但 API 封闭，暂不对接** | 环境温度取设备自带传感器；预留动环接口 |
| 11 | 无线 AC 型号 | **Cisco 9800-L 与 3504 混合** | 双 WLC 驱动：3504 走 AIRESPACE MIB，9800-L 走新 MIB + REST（见 5.6.4） |
| 12 | SNMP 现状 | **部分开启** | 一期前置任务：批量开启并统一策略（见第 9、10 章） |
| 13 | 虚拟化规模 | **大量虚机/云主机**（V0.5 确认） | 新增虚机/云主机模型，宿主机 1─N 虚机；清单经 vCenter/云平台 API 同步（二期，见 5.5.1/5.6.3） |
| 14 | 工单/ITSM | **无 ITSM，平台内置轻量事件单**（V0.5 确认） | 三期实现：报障->分派->处理->反馈，与告警/巡检联动（12.2-6） |
| 15 | 堡垒机 | **已有 JumpServer**（V0.5 确认） | 5.17.2/5.17.3 服务器侧会话与登录审计直接对接其 API，平台不自建会话采集组件（12.2-13） |

### 11.2 遗留问题（后续确认，不阻塞一期启动）

1. 工单/ITSM：（V0.5 已定）公司无 ITSM，三期平台内置轻量事件单，见 11.1-14。
2. 飞书认证具体方式：自建应用 OAuth2 扫码登录，还是飞书_LDAP 同步账号？（建议前者，见 5.2.1）
3. 采集器分片落地：是否在每个地区机房各部署一个采集器节点？（影响跨地区网络路径与管理段打通策略）
4. newapi 侧配额：AI 场景的 QPS/Token 预算，是否为平台单独建分组与令牌？
5. Cisco 3504 已处生命周期后期（EOS），是否有替换时间表？（影响双驱动长期维护投入）
6. 是否需要设备"占用/预约/共享"流程？（测试机/共享设备复用场景，见 5.17 评审补充）
7. 服务器登录会话审计数据源：已有堡垒机，还是需部署轻量 agent？（见 5.17.2/5.17.3）
8. 是否新增独立"变更管理流程"模块（申请→审批→实施→验证→关闭），还是由 NCM 变更留痕扩展？（V0.4 已定：轻量变更单，见第 9 章二期与 7.2.1 apps/change）
9. 虚拟化规模：（V0.5 已定）大量虚机/云主机，虚机模型 + vCenter/云 API 同步进二期，见 11.1-13。

---

## 12. 评审补充项（供方案作者再次 Review）

> 以下为架构评审时发现的遗漏/建议项。**核心补充（12.1）已写成正式需求章节 5.17**。V0.4 已由方案作者逐项 Review，结论见各表"评审结论"列：采纳项已同步落入第 5/6/9 章对应位置；"待确认"项转入 11.2 遗留问题。

### 12.1 核心补充：设备使用与共享（已新增 5.17 章节；V0.4 Review 通过，边界修正见 5.17 头部）

| 项 | 说明 | 涉及章节 |
|---|---|---|
| 占用状态管理 | 动态占用（空闲/占用中/预约中/锁定维护）与生命周期状态分离；预约冲突检测、超时释放提醒、占用期告警降噪 | 5.17.1 |
| 登录状态与会话监测 | 区分"在线（可达）"与"有人正在登录"；采集设备 VTY 会话、服务器 who/last、堡垒机会话 | 5.17.2 |
| 远程登录审计（Login Event） | 谁 + 源 IP + 何时 + 登了哪台 + 成败，覆盖绕开平台直接登录的行为 | 5.17.3 |
| 系统资源统计与可用检索 | 逻辑资源（CPU/内存/磁盘/GPU/空闲端口/PoE/IP）与"空闲且可用"组合检索 | 5.17.4 |
| 共享资源池 | 空闲/占用/预约三池视图，普通员工可查可申请 | 5.17.5 |

### 12.2 其他遗漏项（建议评估纳入，待确认）

| # | 项 | 说明 | 建议优先级 | 评审结论（V0.4） |
|---|---|---|---|---|
| 1 | 设备借用/外借登记 | 借出、归还、责任人变更、离职交接 | P2 | **采纳**：并入 5.5.7 资产生命周期，不单拆模块（三期） |
| 2 | 虚拟化资源建模 | 300+ 服务器是否含虚机；建议补"虚机"模型并关联宿主机 | 待确认 | **采纳**（V0.5 已确认大量虚机/云主机）：虚机模型 + 宿主机关联 + vCenter/云 API 同步，二期（见 11.1-13） |
| 3 | License/维保/服务合同管理 | 防火墙/WLC License 到期、维保合同（现仅保修提醒） | P2 | **采纳**：并入 5.5.7 扩展（三期） |
| 4 | 固件升级作业计划 | 批次、窗口、回滚；与 5.5.4 版本一致性联动 | P2 | **采纳**：三期路线图 |
| 5 | 变更管理流程 | 申请→审批→实施→验证→关闭（非仅配置留痕） | P1 | **采纳**：轻量变更单（apps/change），二期；不做重型 ITSM |
| 6 | 工单/故障流程 | 用户报障→分派→处理→反馈（与 11.2-1 合并确认） | P1 | **采纳**（V0.5 已确认无 ITSM）：三期平台内置轻量事件单（11.1-14） |
| 7 | 值班排班/交接班 | 排班表、on-call 轮值、交接班记录 | P2 | **采纳**：三期，联动 5.7 告警升级 |
| 8 | Runbook/运维知识库 | 日常操作手册与故障处理步骤独立管理 | P2 | **采纳**：与 5.13.2 知识库 RAG 同源建设（Runbook 即 RAG 语料），四期 |
| 9 | 交换机端口容量统计 | 空闲端口数，支撑"新设备能接哪" | P1 | **采纳**：提前至二期（接口清单已采集，仅需统计口径） |
| 10 | 实际功耗采集 | PDU 电表/PoE 实测，替代额定功率估算 | P2 | **采纳**：三期；前提为智能 PDU（SNMP 可采） |
| 11 | 容量预测 | 机柜/带宽/端口打满时间预测 | P2 | **采纳**：四期（依赖 ≥6 个月数据积累 + AI） |
| 12 | PDU 远程电源控制 | 远程开/关机、断电重启（测试机远程重启刚需） | P2 | **采纳**：三期；智能 PDU 硬件前提 + 高危操作强审批 |
| 13 | 服务器登录审计数据源 | 需明确 agent/堡垒机方案（node_exporter 不采会话） | 见 5.17.2 | **已定**（V0.5 确认已有 JumpServer）：直接对接 JumpServer API，无需新组件（5.17.2） |
| 14 | 告警/巡检结果去重 | 5.8.3 巡检异常与监控告警存在重复 | P1 | **采纳**：共用告警事件表，去重键=设备+检查项/规则（已写入 5.8.3） |
| 15 | 时区 | 多地区多机房：建议 UTC 存储 + 前端本地化 | P1 | **采纳**：已写入第 6 章非功能需求，一期定死 |
| 16 | 告警 SLA 量化 | "重要告警 X 分钟内确认/处理"目标值 | P1 | **采纳**：已写入第 6 章（默认值可配，超时触发升级） |
| 17 | 平台容灾 RTO/RPO | 补充定义与恢复演练；采集器跨地区容灾 | P1 | **采纳**：RPO≤24h/RTO≤4h + 半年演练；采集器故障降频接管（第 6 章） |
| 18 | 备份安全 | 配置备份异地副本、加密、存储位置 | P1 | **采纳**：并入第 6 章容灾行 + 5.11 敏感信息处理 |

### 12.3 文档内部待澄清问题

| # | 问题 | 涉及章节 | 答复（V0.4） |
|---|---|---|---|
| 1 | "状态"字段语义：生命周期状态与占用状态需显式拆分，避免误读 | 5.5.2 / 5.17.1 | 已拆分：5.5.2 增加占用状态字段并注明适用范围（仅测试/研发/共享池设备） |
| 2 | 300+ 服务器是否含虚机，决定 CMDB 模型与 node_exporter 覆盖范围 | 5.6.3 | 已确认（V0.5）：大量虚机/云主机；已装 exporter 的直接复用，未装的经 vCenter/云 API 采集（11.1-13） |
| 3 | 离线判定口径统一：SNMP 连续 N 次失败 vs last scrape 超时 | 5.6.2 / 5.6.3 | 已定并写入 5.6.2：SNMP 连续 3 次失败且 ICMP 失败=离线；last scrape 超 2 倍间隔且 ICMP 失败=采集异常 |
| 4 | 9800-L"多接口"：单台多口还是多台堆叠/集群，影响拓扑与 AP 归属建模 | 5.6.4 | 按单台多网口建模；若为 HA 对则以虚拟 IP 建模主设备、AP 归属 active WLC，不影响双驱动方案 |
| 5 | 原始指标 90 天是否支撑 AI 动态基线（需延长保留或降采样） | 6 / 5.13 | 结论：足够 v1（动态基线需 ≥30 天训练数据，90 天原始+1 年 5 分钟聚合满足）；跨年季节性对比需求出现时再延长保留 |
| 6 | 一期开放 API 给 AI 的只读 Token：限流与最小权限需一期确定 | 5.16 | 已定并写入 5.16：一期即提供只读 Token、限定查询端点、限流 60 次/分钟、独立调用审计 |

---

## 附录 A：核心数据模型关系（简图）

```
地区 1─N 机房(站点) 1─N 机柜 1─N 设备(资产) 1─N 接口
                                    │
                    ┌───────────────┼────────────────┐
                    N               1                N
                 配置版本        告警事件          日志记录
                 指标(时序)      巡检记录          变更历史

接口 N─N 接口（通过 线缆 连接记录）
设备 N─N 设备（通过 LLDP邻居/拓扑连线）
设备 1─N IP地址（IPAM）
设备 1─N 占用记录(DeviceUsage) —— 用户/时间/用途/审批单（见 5.17.1，评审补充）
设备 1─N 登录事件(LoginEvent)  —— 用户/源IP/时间/成败（见 5.17.3，评审补充）
设备 N─1 设备模型（动态属性模板）
用户 N─N 角色 N─N 权限点
```

## 附录 B：网络设备日巡检模板（示例）

| # | 检查项 | 检查方式 | 正常标准 | 异常级别 |
|---|---|---|---|---|
| 1 | 设备在线状态 | SNMP/ICMP 探活 | 全部在线 | 紧急 |
| 2 | CPU 使用率 | SNMP | < 80% | 重要（>90%紧急） |
| 3 | 内存使用率 | SNMP | < 80% | 重要 |
| 4 | 温度 | SNMP | < 阈值（按型号） | 重要 |
| 5 | 电源模块状态 | SNMP | 正常 | 紧急 |
| 6 | 风扇状态 | SNMP | 正常 | 紧急 |
| 7 | 接口错包增量 | SNMP 两轮差值 | 增量 < 阈值 | 一般（持续增长升级重要） |
| 8 | 接口 up/down 抖动 | 事件统计 | 无频繁翻转 | 重要 |
| 9 | 光模块光功率 | SNMP | 收发光在型号标称范围 | 一般（光衰预警） |
| 10 | 防火墙会话数水位 | SNMP | < 80% 容量 | 重要 |
| 11 | HA 状态 | SNMP/CLI | 主备正常 | 紧急 |
| 12 | 离线 AP 数 | WLC 采集 | 0 | 重要 |
| 13 | 配置变更检测 | NCM diff | 无未预期变更 | 重要 |

## 附录 C：机柜可视化交互原型说明（文字稿）

```
┌──────────── 机柜 A01 (42U) ────────────┐   待上架设备区
│ U42 │▓▓▓▓▓▓ 预留 ▓▓▓▓▓▓│ (黄色)      │  ┌──────────────┐
│ ... │                                │  │ Cisco WLC 1U │
│ U38 │████ FW-01 (FortiGate 2U) ████ │  │  拖拽我 ↑    │
│ U37 │████ 告警(红点闪烁)        ████ │  ├──────────────┤
│ U36 │████ SW-Core01 (H3C 1U)  ████ │  │ 服务器 4U     │
│ ... │  ...                          │  └──────────────┘
│ U3  │████ ODF-01 (1U)          ████ │
│ U2  │████ PDU/理线架(灰色)     ████ │
│ U1  │████                        ████ │
└────────────────────────────────────────┘
  已用 28U/42U │ 电力 6.2kW/10kW │ 设备 15 台
```

- 拖拽"待上架设备"至目标 U 区释放 → 占据对应高度，冲突（重叠）时目标区红色提示不可放；
- 设备块默认绿色，实时告警黄色/红色，离线灰色；悬停显示摘要浮层，点击右侧滑出 360° 详情；
- 底部条实时统计已用 U、电力合计（按设备额定功率累加）、设备数。

---

> **评审记录**
>
> | 版本 | 日期 | 修订内容 | 评审人 |
> |---|---|---|---|
> | V0.1 | - | 初稿 | - |
> | V0.2 | - | 依据 12 项决策问答更新：Django 模块化（新增 7.2.1）/全自研/LLM 走自建 newapi/飞书生态/深信服 AC/地区-机房两级站点模型/9800-L+3504 双 WLC/网络设备 500+/服务器 300+/动环暂不对接/SNMP 部分开启；第 11 章改为决策记录 | - |
| V0.3 | - | 架构评审补充：新增"设备使用与共享"模块（5.17）与评审补充项汇总（第 12 章）；同步更新模块清单（4.2）、路线图（第 9 章）、遗留问题（11.2）、数据模型（附录 A）；待方案作者再次 Review | - |
| V0.4 | - | 方案作者 Review V0.3 补充项：5.17 采纳并加三点边界修正（占用范围限定/降噪复用静默机制/服务器会话采集按例外处理）；12.2 十八项：采纳 15、待确认 2、已定 1；12.3 六项逐项答复；同步更新 4.1/5.2.2/5.5.2/5.5.3/5.6.2/5.6.5/5.7/5.8.3/5.13/5.16/第6章/7.2.1/第9章/11.2/第12章 | - |
| V0.5 | - | 三项新决策固化（11.1 扩至 15 项）：大量虚机/云主机（新增虚拟化分层建模与 vCenter/云 API 同步，二期）；无 ITSM（三期内置轻量事件单）；已确认 JumpServer（5.10/5.17/7.3 改为直接对接）；更新 1.1/4.2/5.5.1/5.6.3/路线图/11.2/12.2/12.3 | - |
