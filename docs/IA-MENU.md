# nops 菜单 IA 规划（角色域分组）v1

> 目标：把"平铺功能菜单"升级为"按角色域分组 + 可折叠"的导航，每个域标注主要使用角色；
> 未来按 RBAC 菜单权限（perm 的 menu 域字段已就位）逐用户只渲染其职责分组。
> 本文档同时是"角色 × 关注点 → 数据来源 → 现状/规划"的对照表，作为后续模块开发顺序的依据。

## 0. 落地状态

- ✅ 2026-09-03 已把前端侧栏重组为 6 个域分组 + 工作台/日志/系统管理单项（layout.vue）；
- ⏳ 规划位为禁用占位（el-menu-item disabled），仅提示"后续将做什么"，不提供假入口；
- 🚧 动态菜单（按登录用户角色只显示相关分组）依赖 RBAC 菜单权限树下发，见 §4。

## 1. 角色 × 关注点矩阵

图例：列 = 角色关注点；内容 = `数据来源 / 现状（页面·Tab）→ 规划（依赖/阶段）`。

### 1.1 网络管理员
| 关注点 | 现状（页面·入口） | 规划 / 依赖 |
|---|---|---|
| IP 网段 / VLAN 范围 / 可用地址 | IPAM：VLAN/Subnet/IP 台账、占用检索（/ipam） | 可用地址实时过滤增强（依赖 VM/ARP 联动） |
| 路由状态 / OSPF·BGP 邻居 | 设备 360° 技术概览 + **网络总览页（M2026-09-04b，汇总视图）** | 路由快照 diff/前缀变化检测（复用快照表做历史比较） |
| ACL / NAT / 公网 S/D | 360 TechSnapshot 入口（acl/ipsec 快照已建模） | fortigate/asa 采集驱动：access-list、vip/iptables NAT、ipsec tunnel 状态表 |
| 链路状态 / 出入向流量 / 链路质量 | 网络总览页链路分区（下行/高错包+光功率）+ 设备 360 接口表 | 链路质量时序(错包/时延趋势图)，依赖 stat 历史采样；出入向流量排行 |
| 无线状态 | AP 台账同步 + 360 AP 卡 | WLC 无线总览（信道/客户端/功率分布、漫游、RSSI） |
| 各地区的网络规划 | 地区/机房树 + 设备分组 | 规划画布：地区→VLAN 段→网关→设备分配视图（IPAM 增强） |
| 网络割接 | 变更管理（change_type=network 全流程 + 审批） | 割接窗口联动告警静默（alert_silence 未接）、NCM 变更事件关联 |

### 1.2 系统管理员
| 关注点 | 现状 | 规划 / 依赖 |
|---|---|---|
| OS/内核/虚拟化/裸机/设备形态/硬件配置 | 设备台账 + 动态模型（服务器/虚拟机 model + attrs）、360 概览 | 系统域清单视图：按形态(物理机/虚机/容器未来)分组的硬件/OS 汇总表 |
| 用途归类 | 设备 usage_tag + 分组（动态规则）+ Business | 业务-设备矩阵页（DeviceBusiness 已建表，接 API 与编辑） |
| 常用工具版本 | — | 软链：版本一致性页扩到"工具/中间件/Agent 版本"（新采集或台账录入） |
| 应用服务状态 / 系统负载 | 采集器状态 + 接口/CPU 指标（monitor VM 已收） | 系统资源页：CPU/内存/磁盘/进程 TOP、服务存活探测（新 agent 或 SSH 探活） |
| 巡检状态 | 巡检中心（模板/任务/报告） | 区分系统域模板；结果自动转事件单 |
| 系统日志 | 日志中心（syslog 检索） | 关键字规则告警模板化 |
| 备份状态/日志 | — | 备份任务台账 + 结果采集（备份系统 API 或脚本上报，新模块） |

### 1.3 机房运维管理员
| 关注点 | 现状 | 规划 / 依赖 |
|---|---|---|
| 机房温湿度 | — | 环境传感器接入（SNMP UPS/机房动环网关），告警+图表（缺采集源，阶段 3） |
| 机柜位置/空间状态 | 机房管理：机柜 U 位占用可视化、可用 U 检索 | 空间利用率总览（每机柜已用/可用/规划） |
| 设备位置状态 | 设备台账（机柜/U/拖拽上架换位下架）+ 平面图 | — |
| 机房布局 | 平面图 DIY 编辑器 | 楼层分区/桥架图层（阶段 3） |
| 布线状态 | Cable 表（dcim）无独立页 | 布线视图：端口↔端口连线（阶段 3，依赖拓扑/LLDP 联动） |
| 监控/维修状态 | 摄像头—;维修→生命周期 repair + 资产事件 | 上下架/维修工单（阶段 2，见 §1.4） |

### 1.4 审计员（含日常单据）
| 关注点 | 现状 | 规划 |
|---|---|---|
| 系统操作日志 | 系统管理→审计日志（AuditLog API /system） | 独立"操作审计"页：时间线 + 变更前后值 diff |
| 巡检单 / 故障运维单 / 上下架单 | 巡检报告 + 事件单 + 资产生命周期事件(含 borrow/return 等) | 工单一体化视图：按设备/机房聚合各单据 |
| 实施步骤概览 / 处置步骤 | 变更单 content/rollback_plan、事件单处理结果 | SOP 库（复用处置/回滚步骤沉淀），事故复盘时间线 |
| 概览看板 | 工作台 | 审计统计：单据量/超时率/变更成功率 |
| 日常温湿度记录 | — | 环境日志归档（同 §1.3 采集） |

### 1.5 监控管理员
| 关注点 | 现状 | 规划 |
|---|---|---|
| 软硬件/链路/系统/物理机/虚拟机告警 | 告警中心：规则(metric/state)、dedup、ack/resolve | 规则模板库：链路错包/高流量、虚机状态等开箱模板 |
| 温湿度告警 | — | 环境监控规则（依赖 §1.3 传感器） |
| 告警通知/抑制/循环策略 | NotifyChannel（飞书）+ alert 规则引擎 | 抑制窗口（变更联动）、维护窗、路由到不同渠道、升级策略 |

### 1.6 安全管理员
| 关注点 | 现状 | 规划 |
|---|---|---|
| 安全合规性 | 设备台账（防火墙类驱动状态）+ 登录事件表(LoginEvent 已建) | 基线核查模块（三期遗留项）：模板-检查-整改单闭环 |
| 漏洞状态/修复 | — | 漏洞库对接（扫描器 API 或导入），绑定设备/版本 |
| 批处理 | 自动化运维脚本可覆盖（批量） | 批量修复工作流（脚本+审批复用 automate） |
| 安全日志 | 日志中心 + syslog 规则 | 登录审计视图（LoginEvent 已建，接 UI） |

### 1.7 桌面/接入管理员
| 关注点 | 现状 | 规划 |
|---|---|---|
| 接入交换机口↔工位/人 ↔ IP/MAC/位置 | 交换机接口表(DeviceInterface)、AP/终端模型部分存在 | 终端定位模块：MAC-IP 学习(ARP 采集)+ 工位-端口-人 档案；私接交换机检测（LLDP 邻居非直连 + MAC 回环分析） |
| 私接交换机检测 | — | 同上层：端口下挂多终端/出现非预期 LLDP 邻居告警 |

## 2. 补充角色（用户未列，建议考虑）

| 角色 | 关注点 | 归属/规划 |
|---|---|---|
| 领导/管理层 | 全局态势大屏：资产规模、告警趋势、变更成功率、风险设备 | 工作台升级 KPI 区 + 大屏路由（复用各域汇总 API） |
| 值班员 NOC | 告警→事件→处置 值班台、交接班 | 事件单增强：值班视图 + 升级策略（挂账：通知/升级未接） |
| 资产管理/采购 | 维保/合同(License)、资产生命周期、借还 | 已交付设备运营+生命周期；补充采购单关联 |
| 存储/备份管理员 | 存储设备、备份任务 | 并入系统域规划（§1.2 备份项），存储设备建模走动态模型 |
| 无线管理员 | WLC/AP/漫游/信道 | 并入网络域无线总览（§1.1） |
| 流程管理员/合规审计 | 单据审批流、模板 | 走 RBAC 分组，变更/事件审批已就位 |

## 3. 菜单树（本次落地 + 规划位标注）

```
总览
  工作台                                  [全角色]
监控与告警（值班/监控管理员）[Bell]
  告警中心 / 事件单·值班台 / 巡检中心
  ⌛ 环境温湿度告警联动（规划）
网络（网络管理员）[Connection]
  IP·VLAN·地址 / 网络拓扑 / 配置备份与 diff / 网络总览（路由·链路·无线）[已上线 M2026-09-04b]
  ⌛ NAT/ACL 策略 · 链路质量时序（扩展位已预留）
资产与机房（机房·资产·桌面管理员）[OfficeBuilding]
  设备台账 / 设备运营 / 机房与机柜
  ⌛ 上下架·维修工单 / 布线视图（规划）
流程与自动化（变更/作业执行）[Promotion]
  自动化运维 / 变更管理（含网络割接）
安全与合规（安全/审计）[Lock]
  操作审计（全站留痕 + 前后 diff）[已上线 M2026-09-04a]
  ⌛ 登录审计视图 / 安全基线·漏洞（规划，登录审计见 system LoginEvent 表）
日志中心 [Tickets]（网络/系统/安全日志统一检索）
系统管理 [Setting]（用户·角色·凭据·通知·审计日志）
```

> 说明：设备台账/360 是各域通用入口，放在"资产与机房"；巡检中心同时被监控/机房/审计引用，按主责放"监控与告警"。
> 规划位一律禁用（不可点），避免"空页面"。

## 4. 动态菜单路线（RBAC 按角色渲染）

1. ✅ 2026-09-04 已落地：`/auth/me` 返回 `perm_codes`（common.permissions.user_perm_codes），layout 按「菜单项 ↔ 门禁权限码（any-of，支持 `dcim.*` 前缀）”过滤，空权限兜底全显（后端接口仍按 RbacPermission 拦截）。演示账号 `auditor/NopsTest@2025`（角色“审计员”，仅 system.audit.view）登录只见 工作台 + 操作审计。
2. 现状说明：平台角色权限普遍较宽（net_ops 正则放行多域、只读角色带全域 view 码），动态菜单的价值随角色细化而显现；menu 域字段（category=menu）已供授权界面使用。
3. 远期：菜单树表 + 角色-菜单授权界面（可拖树）——开发顺序建议：① 静态域分组（已做）→ ② /auth/me 权限过滤（已做）→ ③ 授权界面。
4. 配套：菜单项→权限码映射集中在 `frontend/src/layout.vue` MENU 常量；新增页面时按“该页 viewset 的 required_perm”登记即可自动纳入过滤。
5. （2026-09-05 细化）菜单过滤改用独立**导航权限点 menu.\***（home/monitor/net/asset/dcim/workflow/security/log/sysadmin，view 级）——解决多个页面共用功能码导致的"角色菜单趋同"；功能码继续由后端 required_perm 拦截。内置演示角色：网络管理员(net_demo)/系统运维(sys_demo)/机房运维(dcim_demo)/审计员(auditor)，密码均 NopsTest@2025；角色-菜单对照见 HANDOVER §19。维护三处对齐：layout.vue MENU ↔ init menu.* ↔ viewset required_perm。

## 5. 后续开发阶段建议（对应菜单规划位）

- **阶段 2（纯后端/视图，无需新采集）**：网络路由/NAT 汇总视图（复用 cmdb_routingneighbor、RouteTableSnapshot、TechSnapshot）、操作审计独立页（AuditLog 全量+diff）、业务-设备矩阵页（DeviceBusiness）、LoginEvent 登录审计视图、工单聚合视图（事件/变更/资产生命周期事件按设备聚合）。
- **阶段 3（需要采集/接入）**：环境温湿度（动环/传感器）；fortigate/asa 的 ACL/NAT/IPSec 采集驱动（写 TechSnapshot）；服务器 OS/内核/服务/备份/私接检测代理；WLC 无线总览。
- 依赖采集源的规划位，落地时需先补 ER 表/采集 driver，再开菜单。

## 6. 改动文件
- `frontend/src/layout.vue`：域分组二级菜单 + 规划位占位（本次）
- 本文件 docs/IA-MENU.md：角色矩阵与开发规划
